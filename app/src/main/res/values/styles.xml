<resources>

    <!-- Base application theme. -->
    <style name="AppTheme" parent="Theme.AppCompat.Light.NoActionBar">
        <!-- Customize your theme here. -->
        <item name="colorPrimary">@color/colorPrimary</item>
        <item name="colorPrimaryDark">@color/colorPrimaryDark</item>
        <item name="colorAccent">@color/colorAccent</item>
        <item name="android:windowBackground">@drawable/qs_background</item>
    </style>

    <style name="buttonVerDetalhes" parent="Widget.AppCompat.Button.Colored">
        <item name="android:height">48dp</item>
        <item name="android:width">88dp</item>
        <item name="android:elevation">4dp</item>
        <item name="android:textColor">#FFFFFF</item>
        <item name="android:colorButtonNormal">@color/colorAccent</item>
        <item name="android:textAllCaps">false</item>
        <item name="android:text">Ver Detalhes</item>
    </style>

</resources>
